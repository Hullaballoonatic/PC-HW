import java.util.*;

import static java.lang.System.out;

class Main {
    private static final Scanner in = new Scanner(System.in);
    private static int T = in.nextInt(), N;

    private static int backtrack(int P, int R) {

    }

    public static void main(String[] args) {
        while(T-->0) {
            N = in.nextInt();
            out.println(backtrack(in.nextInt(), in.nextInt()));
        }
    }
}